__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.DS_Store

# Vendored ChatDev 1.x: we keep the source that actually runs (camel/, chatdev/,
# CompanyConfig/, run.py ~ 340KB) so this study stays reproducible even if the
# upstream fork disappears -- which is exactly what happened to ChatDev 1.x in
# the official repository. Everything below is demo output and web assets.
vendor/ChatDev1x/.git/
vendor/ChatDev1x/WareHouse/
vendor/ChatDev1x/misc/
vendor/ChatDev1x/online_log/static/
vendor/ChatDev1x/online_log/*.log

# stray clone of ChatDev 2.0, kept only to show the rewrite; not used
vendor/ChatDev/
