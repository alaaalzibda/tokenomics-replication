# No external pip dependencies; this project uses only the Python 3 standard library.
# Optional (OS package, not pip): for the optional GUI, install tkinter
# e.g., on Debian/Ubuntu: sudo apt-get install python3-tk
